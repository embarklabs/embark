/* globals describe, it */
const assert = require('assert').strict;

describe('Embark Transaction Manager', () => {
  it('should have tests', (done) => {
    assert(false, 'No tests yet on embark-rpc-manager');
    done();
  });
});


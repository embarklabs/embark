/* globals describe, it */
const assert = require('assert').strict;

describe('Testing', () => {
  it('should have tests, please write them', (done) => {
    expect(true).toBe(true);
    done();
  });
});
